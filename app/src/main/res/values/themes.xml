<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.TvApk" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <!--
        Splash theme: extends our app theme but enables the platform splash
        screen API (API 31+) and the AndroidX core-splashscreen compat shim
        for older versions. The splash icon is the launcher foreground; the
        background uses our deep blue. The Compose root then takes over with
        an animated logo zoom + neon glow.
    -->
    <style name="Theme.TvApk.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="postSplashScreenTheme">@style/Theme.TvApk</item>
    </style>
</resources>
